---
title: Sistema de Alertas
description: Sistema de notificaciones personalizables que reemplaza los alert() y confirm() nativos del navegador
---

import { Card, CardGrid } from '@astrojs/starlight/components';

# Sistema de Alertas (`alertas.js`)

El sistema de alertas proporciona notificaciones personalizables y modales de confirmación que reemplazan automáticamente las funciones nativas `alert()` y `confirm()` del navegador.

## 🚀 Inicio Rápido

```javascript
// El sistema se inicializa automáticamente en gloryRecarga
// No requiere configuración manual
```

## 📋 Funcionalidades

### Alertas Personalizadas
Reemplaza automáticamente `window.alert()` con notificaciones visuales elegantes.

```javascript
// Uso directo - reemplaza alert() nativo
alert('¡Operación completada exitosamente!');
```

### Confirmaciones Modales
Reemplaza automáticamente `window.confirm()` con modales de confirmación.

```javascript
// Uso directo - reemplaza confirm() nativo
const resultado = await confirm('¿Estás seguro de eliminar este elemento?');
if (resultado) {
    // Usuario confirmó
    eliminarElemento();
}
```

## 🎨 Características Visuales

### Estilos de Notificación
- **Alertas**: Notificaciones temporales (3 segundos) con animación de entrada/salida
- **Confirmaciones**: Modales con botones "Confirmar" y "Cancelar"
- **Animaciones**: Transiciones suaves con `notification-fade-out`
- **Posicionamiento**: Fijo en la esquina superior derecha

### Estructura HTML Generada

```html
<!-- Contenedor principal -->
<div id="glory-notification-container">
    <!-- Notificación individual -->
    <div class="notificacionPersonalizada">
        <div class="notification-content">
            Mensaje de la notificación
        </div>
        <!-- Solo para confirmaciones -->
        <div class="notification-buttons">
            <button class="botonSecundario">Cancelar</button>
            <button class="botonPrincipal">Confirmar</button>
        </div>
    </div>
</div>
```

## 🔧 Configuración Avanzada

### Variables Globales
```javascript
// Control de debug (opcional)
window.gloryDebug = true; // Habilita logs en consola
```

### Integración con Modales
El sistema se integra automáticamente con el sistema de modales existente:
- Las confirmaciones activan el fondo modal (`modalBackground`)
- Se respeta el z-index de otros componentes modales

## 📝 API Técnica

### Funciones Principales

#### `inicializarAlerta()`
Inicializa el sistema de alertas y reemplaza las funciones nativas.

```javascript
// Se ejecuta automáticamente en gloryRecarga
// También puede llamarse manualmente si es necesario
inicializarAlerta();
```

### Eventos

#### `gloryRecarga`
El sistema se reinicializa automáticamente cuando ocurre este evento.

```javascript
document.addEventListener('gloryRecarga', inicializarAlerta);
```

## 🎯 Casos de Uso

### Notificaciones de Éxito
```javascript
alert('Los cambios han sido guardados correctamente.');
```

### Confirmaciones de Acción Destructiva
```javascript
const confirmar = await confirm('Esta acción no se puede deshacer. ¿Continuar?');
if (confirmar) {
    await eliminarDatosPermanente();
}
```

### Validaciones de Formulario
```javascript
if (!formularioValido) {
    alert('Por favor, complete todos los campos requeridos.');
    return false;
}
```

## 🔄 Compatibilidad

- ✅ **Reemplazo Automático**: Funciona sin cambios en código existente
- ✅ **Promises**: Las confirmaciones devuelven Promises para async/await
- ✅ **Accesibilidad**: Soporte básico para lectores de pantalla
- ✅ **Responsive**: Se adapta a diferentes tamaños de pantalla
- ✅ **Integración**: Compatible con otros sistemas Glory (modales, etc.)

## 🐛 Solución de Problemas

### Problemas Comunes

1. **Alertas no aparecen**: Verificar que `gloryRecarga` se dispare correctamente
2. **Estilos no se aplican**: Revisar que los estilos CSS estén cargados
3. **Confirmaciones no funcionan**: Verificar compatibilidad con async/await

### Debug
```javascript
// Habilitar logs de debug
window.gloryDebug = true;

// Verificar inicialización
console.log('Alertas inicializadas:', typeof window.alert.isCustom);
```