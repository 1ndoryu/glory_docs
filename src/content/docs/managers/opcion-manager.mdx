---
title: OpcionManager
description: Registro, resolución y sincronización de opciones del tema con OpcionManager
---

## ¿Qué es OpcionManager?

OpcionManager gestiona opciones del tema con validación, sanitización y UI automática. Permite registrar opciones desde código y acceder a sus valores.

## Uso básico

**Uso mínimo** - Registrar y usar opción:
```php
use Glory\Manager\OpcionManager;

// Registrar opción
OpcionManager::register('color_primario', [
    'valorDefault' => '#0073aa',
    'tipo' => 'text',
    'etiqueta' => 'Color Primario'
]);

// Obtener valor
$color = OpcionManager::get('color_primario');
```

**Uso avanzado** - Opción ligada a feature:
```php
OpcionManager::register('mi_feature_activada', [
    'valorDefault' => false,
    'tipo' => 'checkbox',
    'etiqueta' => 'Activar Mi Feature',
    'featureKey' => 'miFeature',
    'seccion' => 'avanzado'
]);
```

¡OpcionManager simplifica la gestión de opciones en WordPress!

---

