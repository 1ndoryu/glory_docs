---
title: Bienvenida
description: Introducción a Glory — qué es, filosofía y primeros pasos
---

Glory es un framework moderno y ligero diseñado para acelerar el desarrollo de temas y funcionalidades avanzadas en WordPress. Su objetivo principal es proporcionar herramientas reutilizables (managers, componentes y utilidades) que permitan escribir código más limpio, modular y fácil de mantener, reduciendo significativamente los tiempos de desarrollo.

## ¿Por qué Glory?

### 🚀 **Acelera tu desarrollo**
- **Componentes preconstruidos**: Más de 40 componentes UI listos para usar (modales, formularios, navegación, etc.)
- **Managers especializados**: AssetManager, PageManager, PostTypeManager y más para tareas comunes
- **Utilidades integradas**: AJAX, logging, cache, email y herramientas de desarrollo

### 🏗️ **Arquitectura sólida**
- **Separación clara**: Framework agnóstico (`Glory/`) vs. código específico del proyecto (`App/`)
- **Modularidad**: Cada componente es independiente y reutilizable
- **Extensible**: Fácil de personalizar y expandir según necesidades del proyecto

### ⚡ **Rendimiento optimizado**
- **Carga inteligente**: Assets se cargan solo cuando son necesarios
- **Cache integrado**: Sistema de cache local para CSS crítico y assets
- **Optimización automática**: Minificación y defer de scripts por defecto

### 🎯 **Desarrollo moderno**
- **Código limpio**: Estructura organizada siguiendo mejores prácticas
- **Documentación completa**: Guías detalladas con ejemplos funcionales
- **Comunidad**: Framework en evolución con actualizaciones regulares

## Filosofía

- Separación clara entre framework (`Glory/`) y código específico del proyecto (`App/`).
- Reutilización: componentes y managers pensados para evitar repetición.
- Agnosticismo: el framework no debe contener lógica de negocio del proyecto.

## Requisitos mínimos

- WordPress (versión según tu entorno de desarrollo).
- PHP 7.4+ (o la versión requerida por tu proyecto).

## Primeros pasos

Después de la instalación, puedes crear rápidamente una página personalizada:

**Uso mínimo** - Página básica con Glory:
```php
<?php
use Glory\Manager\PageManager;

// Define página simple con función de renderizado
function renderizarMiPagina() {
    echo '<h1>¡Glory funcionando!</h1>';
}

PageManager::define('mi-pagina', 'renderizarMiPagina');
```

**Uso avanzado** - Con assets y componentes:
```php
<?php
use Glory\Manager\AssetManager;
use Glory\Components\Modal;

// Define assets automáticamente
AssetManager::defineFolder('assets/');

// Define página con modal
function renderizarDemoAvanzada() {
    $modal = new Modal('demo-modal');
    $modal->setTitle('Hola Glory')->render();
    echo '<button onclick="jQuery(\'#demo-modal\').show()">Abrir</button>';
}

PageManager::define('demo-avanzada', 'renderizarDemoAvanzada');
```

## Estructura relevante (rápida)

- `Glory/` — código del framework (agnóstico).
- `App/` — funciones y configuraciones específicas del tema.
- `App/Config/` — donde defines assets, páginas, post types y opciones.


