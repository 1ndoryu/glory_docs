---
title: Cómo Crear Páginas
description: Guía completa para crear páginas personalizadas usando el sistema PageManager de Glory
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

# 📄 Cómo Crear Páginas con Glory

Glory simplifica la creación de páginas mediante el `PageManager`. Las páginas se definen con un slug y una función handler que genera el contenido HTML.

## 🏗️ Cómo Funciona

1. **Define** páginas en `App/Config/config.php` con `PageManager::define()`
2. **Crea** funciones handler que generan solo el contenido (sin HTML completo)
3. **TemplateGlory.php** se encarga del `<head>`, navegación y footer automáticamente

### Ventajas

- ✅ Sin repetir HTML boilerplate en cada página
- ✅ Header/footer consistentes automáticamente
- ✅ Enfoque en el contenido específico de cada página

## 🚀 Crear tu Primera Página

### Paso 1: Definir la Página

Edita `App/Config/config.php`:

```php
<?php
use Glory\Manager\PageManager;

// Página de contacto
PageManager::define('contacto', 'renderContacto');

// Página "Sobre Nosotros"
PageManager::define('sobre-nosotros', 'renderSobreNosotros');

// Página con parámetros dinámicos
PageManager::define('producto', 'renderProducto');
```

### Paso 2: Crear la Función Handler

Crea la función en cualquier archivo PHP cargado por tu tema (recomendado: `functions.php` o archivos dedicados):

```php
<?php
/**
 * Handler para la página de contacto
 */
function renderContacto() {
    // Obtener datos de opciones si es necesario
    $email = get_option('admin_email');
    $telefono = OpcionManager::get('telefono_contacto');
    ?>

    <div class="contacto-hero">
        <h1>Contáctanos</h1>
        <p>Estamos aquí para ayudarte</p>
    </div>

    <div class="contacto-content">
        <div class="contacto-info">
            <div class="info-item">
                <h3>Email</h3>
                <p><?php echo esc_html($email); ?></p>
            </div>
            <?php if ($telefono): ?>
            <div class="info-item">
                <h3>Teléfono</h3>
                <p><?php echo esc_html($telefono); ?></p>
            </div>
            <?php endif; ?>
        </div>

        <div class="contacto-form">
            <h2>Envíanos un mensaje</h2>
            <?php
            use Glory\Components\FormBuilder;

            echo FormBuilder::inicio([
                'atributos' => ['id' => 'form-contacto']
            ]);

            echo FormBuilder::campoTexto([
                'nombre' => 'nombre',
                'label' => 'Nombre completo',
                'requerido' => true
            ]);

            echo FormBuilder::campoEmail([
                'nombre' => 'email',
                'label' => 'Correo electrónico',
                'requerido' => true
            ]);

            echo FormBuilder::campoTextarea([
                'nombre' => 'mensaje',
                'label' => 'Mensaje',
                'requerido' => true,
                'placeholder' => 'Escribe tu mensaje aquí...'
            ]);

            echo FormBuilder::botonEnviar([
                'accion' => 'procesarContacto',
                'texto' => 'Enviar Mensaje'
            ]);

            echo FormBuilder::fin();
            ?>
        </div>
    </div>
    <?php
}
```

## 📋 Páginas con Parámetros Dinámicos

### Página de Producto Detallado

Para páginas que necesitan parámetros (como ver un producto específico), puedes usar el sistema de query vars de WordPress.

#### Definir la Página

```php
// En config.php
PageManager::define('producto', 'renderProducto');
```

#### Crear el Handler

```php
<?php
function renderProducto() {
    // Obtener el ID del producto desde la URL
    $producto_id = get_query_var('producto_id');

    if (!$producto_id) {
        // Mostrar página 404 o redirigir
        wp_redirect(home_url());
        exit;
    }

    // Obtener datos del producto
    $producto = get_post($producto_id);

    if (!$producto || $producto->post_type !== 'producto') {
        // Producto no encontrado
        render404();
        return;
    }
    ?>

    <div class="producto-detalle">
        <div class="producto-hero">
            <h1><?php echo esc_html($producto->post_title); ?></h1>
        </div>

        <div class="producto-imagen">
            <?php echo get_the_post_thumbnail($producto_id, 'large'); ?>
        </div>

        <div class="producto-contenido">
            <?php echo apply_filters('the_content', $producto->post_content); ?>
        </div>

        <div class="producto-meta">
            <p><strong>Precio:</strong> <?php echo get_post_meta($producto_id, 'precio', true); ?>€</p>
            <p><strong>Categoría:</strong> <?php the_terms($producto_id, 'categoria_producto'); ?></p>
        </div>
    </div>
    <?php
}
```

#### Registrar Query Var

En `functions.php` o en un archivo de configuración:

```php
function agregar_query_vars($vars) {
    $vars[] = 'producto_id';
    return $vars;
}
add_filter('query_vars', 'agregar_query_vars');
```

## 🧩 Crear Páginas con Componentes Reutilizables

### Página de Blog con ContentRender

```php
<?php
function renderBlog() {
    // Obtener parámetros de paginación
    $paged = get_query_var('paged') ?: 1;

    // Configuración de la consulta
    $args = [
        'post_type' => 'post',
        'posts_per_page' => 10,
        'paged' => $paged
    ];

    // Aplicar filtros si existen
    if (isset($_GET['categoria'])) {
        $args['category_name'] = sanitize_text_field($_GET['categoria']);
    }
    ?>

    <div class="blog-header">
        <h1>Nuestro Blog</h1>
        <p>Descubre nuestros últimos artículos</p>
    </div>

    <div class="blog-content">
        <div class="filtros-blog">
            <form method="GET" action="<?php echo PageManager::getUrl('blog'); ?>">
                <select name="categoria">
                    <option value="">Todas las categorías</option>
                    <?php
                    $categorias = get_categories();
                    foreach ($categorias as $cat) {
                        $selected = (isset($_GET['categoria']) && $_GET['categoria'] === $cat->slug) ? 'selected' : '';
                        echo "<option value='{$cat->slug}' {$selected}>{$cat->name}</option>";
                    }
                    ?>
                </select>
                <button type="submit">Filtrar</button>
            </form>
        </div>

        <div class="posts-container">
            <?php
            use Glory\Components\ContentRender;

            ContentRender::print('post', [
                'queryArgs' => $args,
                'publicacionesPorPagina' => 10,
                'paginacion' => true,
                'plantillaCallback' => 'plantillaPostBlog'
            ]);
            ?>
        </div>
    </div>
    <?php
}

// Función de plantilla personalizada
function plantillaPostBlog($post) {
    ?>
    <article class="post-card">
        <div class="post-imagen">
            <?php echo get_the_post_thumbnail($post->ID, 'medium'); ?>
        </div>
        <div class="post-contenido">
            <h2><?php echo esc_html($post->post_title); ?></h2>
            <div class="post-meta">
                <span class="fecha"><?php echo get_the_date('', $post); ?></span>
                <span class="autor">Por <?php echo get_the_author_meta('display_name', $post->post_author); ?></span>
            </div>
            <div class="post-excerpt">
                <?php echo wp_trim_words(get_the_excerpt($post), 30); ?>
            </div>
            <a href="<?php echo get_permalink($post); ?>" class="leer-mas">Leer más</a>
        </div>
    </article>
    <?php
}
```

## 🔧 Páginas Avanzadas

### Página con AJAX y Realtime

```php
<?php
function renderDashboard() {
    // Activar realtime para esta página
    use Glory\Core\GloryFeatures;
    GloryFeatures::enable('gloryRealtime');
    ?>

    <div class="dashboard-header">
        <h1>Dashboard</h1>
        <p>Panel de control en tiempo real</p>
    </div>

    <div id="notificaciones-container"></div>

    <div class="stats-grid">
        <div class="stat-card" data-channel="nuevo_pedido">
            <h3>Pedidos Hoy</h3>
            <span class="numero" id="pedidos-hoy">0</span>
        </div>
        <div class="stat-card" data-channel="nuevo_usuario">
            <h3>Usuarios Nuevos</h3>
            <span class="numero" id="usuarios-nuevos">0</span>
        </div>
    </div>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
        // Cargar datos iniciales
        actualizarEstadisticas();

        // Configurar realtime
        if (window.gloryRealtimePoll) {
            window.gloryRealtimePoll(['nuevo_pedido', 'nuevo_usuario'], {
                intervalMs: 5000
            });

            document.addEventListener('gloryRealtime:update', function(e) {
                actualizarEstadisticas();
            });
        }
    });

    function actualizarEstadisticas() {
        gloryAjax('obtener_estadisticas_dashboard')
            .then(function(response) {
                if (response.success) {
                    document.getElementById('pedidos-hoy').textContent = response.data.pedidos_hoy;
                    document.getElementById('usuarios-nuevos').textContent = response.data.usuarios_nuevos;
                }
            });
    }
    </script>
    <?php
}
```


## 🎯 Mejores Prácticas

### Organización del Código

- **Funciones claras**: Una función por página con nombres descriptivos
- **Reutilización**: Crea helpers para lógica común
- **Separación**: Lógica de negocio separada de la presentación

### Estructura Recomendada

```
App/
├── Config/
│   └── pages.php         # Definiciones de páginas
└── Handlers/
    ├── HomeHandler.php   # Lógica de página home
    ├── BlogHandler.php   # Lógica de página blog
    └── ContactHandler.php # Lógica de página contacto
```

¡Listo! Ahora puedes crear páginas con Glory Framework. El sistema PageManager se encarga del enrutamiento, tú solo necesitas definir las funciones que generan el contenido.

## 🔍 Depuración

Para verificar que tus páginas funcionan, puedes usar la función de debug del administrador de WordPress o revisar los logs de Glory.
